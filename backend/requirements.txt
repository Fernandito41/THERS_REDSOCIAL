# Dependencias del backend, fijadas a partir de las versiones observadas y en uso
# real en el código (ver docs/architecture/BACKEND_ARCHITECTURE.md sección 2).
# Solo se listan paquetes que el código actual importa. Flask-SQLAlchemy/SQLAlchemy
# se observaron instalados en un entorno local sin ningún uso en el código - no se
# incluyen aquí para no dar por adoptada una decisión de persistencia sin ratificar
# (BACKEND_ARCHITECTURE.md sección 20, ítem 1).

Flask==3.1.3
flask-cors==6.0.2
Flask-JWT-Extended==4.7.1
